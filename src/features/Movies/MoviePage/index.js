import { MovieTile } from "../../../components/MovieTile";
import { Wrapper } from "../../../Wrapper";
function MoviePage() {

    return (
        <Wrapper>
            <MovieTile />
        </Wrapper>
    );
}

export default MoviePage;